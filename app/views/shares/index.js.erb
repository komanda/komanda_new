$("#shares").append("<%= escape_javascript(render @shares) %>")

<% if @done %>
	$("#more-link").remove()
<% else %>
	$("#more-link").attr("href", "/shares?skip=<%= @skip %>")
	$("#more-link").button("reset")
<% end %>